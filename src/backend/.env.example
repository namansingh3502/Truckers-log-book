SECRET_KEY=replace-me-with-a-secure-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Local default: SQLite. On Vercel, set DATABASE_URL to an external Postgres
# (Neon / Supabase / Railway) — Vercel's filesystem is ephemeral.
# DATABASE_URL=sqlite:///db.sqlite3
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# --- Vercel production overrides ---
# DEBUG=False
# ALLOWED_HOSTS=your-backend.vercel.app
# CORS_ALLOWED_ORIGINS=https://your-frontend.vercel.app
